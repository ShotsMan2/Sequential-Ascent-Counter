# Sequential-Ascent-Counter
A C-based stream processing algorithm that analyzes a sequence of user inputs to quantify local ascending trends ($x_i > x_{i-1}$) until a sentinel termination value (0) is detected.
